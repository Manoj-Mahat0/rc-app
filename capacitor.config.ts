import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.rccontroller',
  appName: 'rc-controller',
  webDir: 'build'
};

export default config;
